flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
openai>=1.54.0
googlemaps==4.10.0
requests==2.31.0

# TikTok scraping (OPTIONAL - app works fine without these)
# Requires Microsoft Visual C++ 14.0+ build tools on Windows
# The app uses simulated TikTok data as fallback if these aren't installed
# Uncomment below only if you want real TikTok scraping:
# TikTokApi==6.5.2
# playwright==1.40.0
